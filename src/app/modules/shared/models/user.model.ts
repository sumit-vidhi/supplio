/**
 * Create a new instance of User
 * @class User
 * @description
 * Defining a user model class for mapping and crating user objects
 */

export class User {
    userId: number;
    id: number;
    title: string;
    body: string;
}
